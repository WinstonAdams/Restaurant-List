<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Restaurant List</title>
  {{!--- 載入 fontawesome --}}
  <script src="https://kit.fontawesome.com/2a610c975d.js" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="/stylesheets/bootstrap.css">
  <link rel="stylesheet" href="/stylesheets/global.css">
</head>

<body>
  <header>
    <!-- navigation -->
    <nav class="navbar navbar-light bg-light">
      <div class="container">
        <a class="navbar-brand" href="/">
          <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png"
            width="30" height="30" class="d-inline-block align-top" alt="餐廳清單">
          {{#if isAuthenticated}}
            {{user.name}}的餐廳清單
          {{/if}}
        </a>
        {{#if isAuthenticated}}
        <div class="text-end mt-2">
          <a href="/restaurants/new"><button class="btn btn-outline-primary fw-bold">新增餐廳</button></a>
          <a href="/users/logout"><button class="btn btn-outline-primary fw-bold">Logout</button></a>
        </div>
        {{else}}
        <div class="text-end mt-2">
          <a href="/users/login"><button class="btn btn-outline-primary fw-bold">Login</button></a>
        </div>
        {{/if}}
      </div>
    </nav>

    <div class="banner"></div>
  </header>

  {{{body}}}

  <script src="/javascripts/bootstrap.js"></script>
  <script src="/javascripts/popper.js"></script>
</body>

</html>